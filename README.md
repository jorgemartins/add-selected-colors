# Add selected colors to Document palette

## how it works

- Select color layers, name them with `color/color-value` format (example: blue/blue-100)
- run the plugin: Add selected colors to... -> Document Colors

